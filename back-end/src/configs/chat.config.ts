const config = Object.freeze({
  NUMBER_OF_USER_DIRECT_MESSAGE: 2,
  DIRECT_MESSAGE: 'Direct message',
  CHANNEL_MESSAGE: 'Channel message',
  EDIT_ROOM_NOTIFICATION: 'The room has just been edited',
  CREATE_ROOM_NOTIFICATION: 'The room have just been created',
  BUCKET_SIZE: 20,
  PAGESIZE_DEFAULT: 1,
});

export default config;
